import java.util.*;

public class ReturnSmallestKey {

    static String returnSmallestKey(HashMap<String, Integer> inputDict, int n)  {
        // Write your code here


        for (String s : inputDict.keySet()){

        }

        return "";
    }


//    public static <K, V extends Comparable<? super V>> Map<K, V> sortByValues(Map<K, V> map) {
//        List<Map.Entry<K, V>> entries = new LinkedList<>(map.entrySet());
//
//        // Custom comparator to compare entries by values
//        Collections.sort(entries, new Comparator<Map.Entry<K, V>>() {
//            @Override
//            public int compare(Map.Entry<K, V> entry1, Map.Entry<K, V> entry2) {
//                return entry1.getValue().compareTox-x(entry2.getValue());
//            }
//        });

}
